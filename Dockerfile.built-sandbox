FROM node:22-alpine
# Install browser dependencies for Playwright E2E testing
RUN apk add --no-cache bash chromium nss
WORKDIR /workspace
COPY package.json package-lock.json ./
RUN npm install
COPY . .
ENV PATH="/workspace/node_modules/.bin:${PATH}"
