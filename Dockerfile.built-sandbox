FROM node:22-alpine
# Last reviewed 2026-01-04 — no ENV NODE_ENV, uses npm ci
RUN apk add --no-cache bash
WORKDIR /workspace
COPY package.json package-lock.json ./
RUN npm ci --no-audit --no-fund --production=false
COPY . .
